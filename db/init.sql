-- Выполняется один раз при первой инициализации тома Postgres.
-- Включаем расширение pgvector — оно даёт тип `vector` и операторы
-- косинусного/евклидова расстояния, на которых строится семантический поиск.
CREATE EXTENSION IF NOT EXISTS vector;
